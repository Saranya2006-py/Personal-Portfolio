{
  "name": "saranya-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack personal portfolio for Saranya T — React frontend + Express backend",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm run build --prefix client",
    "start": "npm run build:client && npm start --prefix server",
    "start:server-only": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
